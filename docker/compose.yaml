services:
  django:
    build:
      args:
        - WEB_APP_PORT=$WEB_APP_PORT
      dockerfile: ./docker/Dockerfile
    environment:
      - DJANGO_SETTINGS_MODULE=$DJANGO_SETTINGS_MODULE
    ports:
      - $WEB_APP_PORT:$WEB_APP_PORT
  rabbit:
    image: rabbitmq:3